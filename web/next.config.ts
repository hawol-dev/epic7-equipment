import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 이미지는 web/public/images/heroes/ 에 자체 호스팅
  // 출처: Smilegate / Epic Seven (Fribbels Optimizer 경유 다운로드)
};

export default nextConfig;
